To launch the web-page, move to the 'mysite' directory by entering the 
following into the command line:

	$ cd Project/mysite

then, enter the following:

	$ python manage.py runserver 


Once that is done, open up an internet browser and go to:

	127.0.0.1:8000
